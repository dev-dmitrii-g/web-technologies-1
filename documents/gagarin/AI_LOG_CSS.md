# AI LOG CSS
1. explain how margin collapse works in the CSS box model
2. how can I resolve a conflict between two selectors without using !important?
3. In what cases would CSS Grid be a better choice than Flexbox?
4. How does the CSS cascade determine which rule wins if I have an inline style, an internal style block, and an external stylesheet all targeting the same element?
5. How to center div?
6. What is clear property?